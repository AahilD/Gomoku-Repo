package controller;

/**
 * @author GROUP 22
 *
 *         This Class will contain all the necessary methods to referee the
 *         match being played, between Players, on the current Board.
 * 
 *         DO NOT WORK ON THIS CLASS, LEAVE IT AS IS FOR NOW
 * 
 */
public class Match
{
	/*
	 * //TODO: private <Game> object private Game game;
	 * 
	 * //TODO: private turnPlayer <Player> (the player who's turn it is) private
	 * Player turnPlayer() {
	 * 
	 * }
	 * 
	 * //TODO: playmove(): boolean (return the
	 * 
	 * 
	 * //TODO: private isWinningMove(): boolean // check if the last move
	 * private boolean isWiningMove(); { makeMove(currentPlayer); }
	 * 
	 * //TODO: private startNewGame(): Game // - USE THE SAME PLAYERS private
	 * startNewGame(); { newGame(); )
	 * 
	 * //TODO: endGame() // - terminate the application. public endGame() {
	 * closeButton.setOnAction(actionEvent -> Platform.exit()); }
	 * 
	 * 
	 * 
	 * //
	 */
}
